# vikasmenon.com — Memory

## Session — 2026-08-24
- **Infrastructure correction:** discovered (via `curl -I` on the live site + `gh api repos/ssupppp/vikasmenon/pages`) that the site runs on **GitHub Pages** (build from `main`, root path, custom domain via `CNAME`), not Vercel — CLAUDE.md's entire branch-model section was wrong and has been corrected. There is no live preview/staging deploy of any kind; `preview` is just a normal branch. Local static server (`python -m http.server`) is the only way to look at unshipped work today.
- **Homepage design pass #2** (first pass — placeholder-box removal + AutoAcquire title — was 2026-08-23):
  - **Removed the hero entirely.** Vikas: "don't want to show off." `.hero`/`.hero-title`/`.hero-lede` deleted from `index.html` + `styles.css`; page now opens directly on the Building list. Added `.visually-hidden` utility + an sr-only `<h1>` so the page keeps exactly one real h1 for SEO/accessibility without any visual footprint. New `.section--first` class gives the first section (`#building`) its own top clearance from the fixed nav (9rem desktop / 7rem mobile) and no top border, replacing what the hero used to provide.
  - **Reordered Sony TV Skill above Rook.** It's a skill built *for* Rook, but strict date-sort (2025 vs Rook's "Mar 2026—") had separated it to the very bottom of the list, below Plumo/Helicopter Game — Vikas flagged this as "the timeline looks off." Fixed by grouping it directly above its parent, overriding pure chronological order for this one relationship.
  - **Fixed the Building→Writing transition.** Root cause: `.build-item:last-child`'s bottom border + the next section's own top border sat close together, reading as an accidental doubled rule. Removed the redundant one — now a single hairline, same section-padding rhythm as every other transition on the page.
  - **Compacted the featured essay.** `.essay--featured` was running at clamp(2rem–2.85rem) title / 1.15rem dek / 4rem margins — dramatically bigger than the regular list (1.5rem/1rem). Brought it down to match the list's scale almost exactly (1.5rem title, 1rem dek, ~1.75rem margins); it's still visually first by position and a border-bottom separator, not by oversized type. Vikas's stated principle: "don't oversize things like headers — they look cheap and showoffish."
  - Verified all four fixes live via local server + browser screenshots. **Not yet committed** — sitting as uncommitted changes on `preview` alongside the docs correction above.
- **Homepage thumbnails — plan, not yet executed.** Vikas wants real thumbnail images back on the Building list (not the empty-box placeholder we removed, and not permanently boxless either). Confirmed one source image per project is enough — same file serves the small homepage crop and the larger detail-page hero crop via `object-fit: cover`, no need to shoot two images per project.
  - **Ready to wire up now, assets already exist:** Plumo (`images/plumo.png`, already used as `plumo.html`'s hero image) and Helicopter Game (`images/helicopter_game.jpg`, already used as `helicopter-game.html`'s hero image). Just needs the homepage `build-thumb` markup added back for these two, pointing at the existing files — no new asset sourcing.
  - **Blocked on real assets from Vikas:** Session Memory, AutoAcquire, Rook, Bowldem for Reddit, Bowldem, Sony TV Skill — none have any image anywhere in the repo. Do not fabricate placeholders for these; wait for Vikas to supply or point at something real (screenshots, logos, a still from the existing unused `images/sony-tv-netflix.mp4` for the Sony TV Skill entry, etc.). Rook is private/no public demo, may end up staying imageless by choice.
- **Session end state — NOTHING IS LIVE.** Confirmed via git: `preview` is 1 commit ahead of `origin/main` (the 2026-08-23 Blue Revision commit, pushed to `preview` only), plus 4 files currently uncommitted locally (`CLAUDE.md`, `MEMORY.md`, `index.html`, `styles.css` — today's design pass #2 + doc corrections). GitHub Pages only ever serves `main`, so none of this session's work — Blue Revision rebuild, design passes, or doc corrections — has reached the live site at vikasmenon.com. Explicit Vikas confirmation ("this is not live right now") — do not describe any of this as shipped/live in future sessions until an actual `ship` (fast-forward main → push) happens.
- **Next session priorities:**
  1. Wire up Plumo + Helicopter Game homepage thumbnails (assets ready, zero blockers).
  2. Get real images from Vikas for the other 6 Building items, or decide which stay imageless.
  3. Commit + push the current uncommitted round (hero removal, reorder, seam fix, featured compact, GitHub Pages doc correction) to `preview`.
  4. When ready: `ship` (fast-forward `main` from `preview`) to actually go live — has not happened yet this entire rebuild.
  5. Mobile visual QA still outstanding (never confirmed via real screenshot, only code review).

## Backlog / Ideas (no formal BACKLOG.md for this project — tracked here)
- **Session Memory** — ✅ done 2026-08-23. New Building entry + `session-memory.html` detail page (self-authored SVG diagram, no external assets). GitHub: github.com/ssupppp/claude-session-memory.
- **FACS videos entry** — raised 2026-08-23, no details yet (what it is, status, links/assets). Ask Vikas before scoping.
- **About-me timeline** — raised 2026-08-23. Sounds like an IA idea for the Now/About section (visual timeline), not a single project entry — needs a scoping conversation (what milestones, how far back, static vs. interactive) before planning.

## Session — 2026-08-23
- Worked on: Full Blue Revision redesign execution, per the Build Sheet decided in an earlier planning session (docs live as Claude Artifacts, not in this repo). Branch: `feat/blue-revision-rebuild` (off `main`, not `preview` — see note below).
- Completed:
  - Retain: homepage Building-row thumbnails (real crop for Port of Hormuz + Fantasy Team Rater, honest hatched placeholder for the rest) via new `.build-rail`/`.build-thumb` CSS; Port of Hormuz + Fantasy Team Rater case-study callouts (`.project-callout`) using existing verbatim lines.
  - Improve: articles.html rebuilt with a two-column TOC (CSS `columns: 2`, no JS) and re-sorted every essay strictly by date, newest first (was previously out of order — AI-Native Games, the oldest essay from Nov 2024, was incorrectly the "featured" pick on both articles.html and the homepage Writing section; fixed to feature the actual newest essay, "Goodbye to the Big Green Button," Mar 2026). Callouts added to AutoAcquire (₹2,068 line), Bowldem for Reddit ("platform constraints" line), Rook ("private, no public demo" status line). Bowldem's player-count stat moved from the homepage row into the page body.
  - Tried and reverted: a "featured essay" treatment (drop cap + pull-quote component) — Vikas called it "pretty cringe," fully removed from both HTML and CSS. Note for future: don't reach for drop-caps/pull-quotes as a default editorial device on this site.
  - Snackula + RedFlag.exe: added "no public link available" status line to both (previously said "Live on Ripple AI" with no way for a visitor to actually reach it). Vikas may upload one or both somewhere public later — swap the line for a real link when he does.
  - New homepage entry: **Session Memory** (`session-memory.html`) — open-source Claude Code skill Vikas published (github.com/ssupppp/claude-session-memory). Own detail page with a self-authored inline SVG diagram.
  - **Kill list — DONE.** All six files removed: `portfolio.html`, `projects.html`, `work.html`, `playbowldem-reddit.html`, `script.js`, `content.txt`. Before deleting the three hub pages, checked every project tile inside them for exclusive inbound links — Plumo, Helicopter Game, Sony TV Skill, and Rizz Messenger only had the hub pages linking to them, so all four were promoted to proper homepage Building entries first (design call: **no separate `/projects` page going forward — homepage Building list is the single source of truth**, consistent with the 3-item nav rule in CLAUDE.md). `sitemap.xml` cleaned of every killed page in the same pass.
  - **Decide items — DONE.**
    - Helicopter Game / Plumo / Sony TV Skill: promoted to Building (see above).
    - **ValuePickr Chat + Rizz Messenger: hidden, not deleted** — Vikas called both "incomplete projects." Removed from homepage Building list and `sitemap.xml`; `rizz-messenger.html` file kept on disk, unlinked (ValuePickr never had its own page). Recoverable later if either project reaches a "done" state worth surfacing.
    - **Resume PDF: unlinked, not deleted.** Read the actual PDF — it's a different professional pitch than the site (traditional live-ops/product-manager resume: "Terrabyte," "Head of Games & Product," D1/D7 retention metrics, no mention of PlotPix.ai or AI at all) vs. the site's AI-native-builder brand. Flagged as a positioning conflict; Vikas chose to unlink rather than rewrite or keep. File stays on disk at `Vikas_Menon_Resume.pdf`, just not linked from the contact strip.
  - **sitemap.xml + llms.txt — DONE, fully rebuilt.** sitemap.xml now lists exactly the live, linked pages (added session-memory.html, autoacquire.html, bowldem-reddit.html; excludes hidden ValuePickr/Rizz Messenger and all six killed files). llms.txt Site Map now points at real anchors (`#building`, `#now`) instead of the dead hub pages; Projects list matches the actual homepage; PlayBowldem entry now links to `bowldem-reddit.html` instead of a GitHub URL; Work section corrected from stale "Head of Games & Platform" to current "Head of Content & AI"; Resume line removed.
- Decisions made:
  - Blue Revision direction locked (see Artifacts, not in this repo). Real-repo work happens on a feature branch (`feat/blue-revision-rebuild`), not this repo's usual `preview` branch, because the scope is large and multi-page — deliberate deviation from the CLAUDE.md branch model, to be reconciled once the rebuild is further along.
  - **No standalone `/projects` or `/work` hub page.** The homepage Building list is the sole canonical listing going forward — two separate grid pages (`portfolio.html`, `projects.html`) had each drifted stale relative to the homepage by the time this session found them. One source of truth, not three.
  - **"Hide, don't delete" is the default for incomplete/borderline content** (ValuePickr Chat, Rizz Messenger, Resume PDF) — Vikas's stated preference: unlink from nav/sitemap, keep the file on disk, recoverable later.
  - **Browser QA (desktop) — DONE.** Verified in Chrome: homepage Building list (all 10 entries, correct order, ValuePickr/Rizz Messenger correctly absent), Session Memory detail page + inline SVG diagram, Now section/contact strip (Resume cleanly removed), articles.html two-column TOC ordering. Mobile viewport resize did not reflect in the browser tool — mobile layout verified by code review only (the `max-width:720px` breakpoint), not by screenshot.
  - **Design pass — DONE.** Vikas flagged "boxes seemed too large" after seeing the real render. Root cause: 7 of 10 Building entries showed an empty hatched-placeholder thumbnail box (only Port of Hormuz + Fantasy Team Rater have real images), which read as unfinished rather than intentional. Fix: removed the placeholder box entirely for all 7 (`build-thumb--empty` CSS rule deleted, markup removed) — those rows are now clean text-only, matching pre-thumbnail style. Also fixed "AutoAcquire — Agent-Driven Ad Engine" (the only subtitle-in-title entry) wrapping and orphaning its status pill onto its own line — shortened to just "AutoAcquire" to match every other entry.
  - **Branch reconciliation — DONE.** No `preview` branch existed in this repo (only `main` + the feature branch, both at the same commit). Vikas chose to adopt `feat/blue-revision-rebuild` as the new `preview`: committed all changes (`5de04d8`), renamed the branch, pushed to origin. Vercel's webhook should auto-deploy the preview URL.
- Next session priorities:
  1. Exercise the live Vercel preview URL to confirm the deploy landed, then `ship` (fast-forward `main` from `preview`) when Vikas is ready.
  2. Mobile visual QA still outstanding — verify the `max-width:720px` breakpoint actually renders correctly on a real small viewport (browser tool's resize wasn't reflecting this session).
  3. Backlog items awaiting Vikas's input: FACS videos entry (not ready, no details yet), About-me timeline (needs a scoping conversation) — see Backlog / Ideas section above.
  4. Flagged but not fixed (pre-existing, sitewide, out of scope for this pass): small metadata type size (~10.5-11px), `--text-tertiary` under WCAG AA contrast on `--bg`.
