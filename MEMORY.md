# vikasmenon.com — Memory

## Session — 2026-08-24 (round 3: content rewrites, fixed invisible-CTA bug, removed Helicopter Game)
- **Fixed a real CSS bug: CTA button text was invisible on any page where `.project-cta` sits inside `.project-detail__content`.** The generic `.project-detail__content a { color: var(--text) }` rule was overriding `.project-cta`'s `color: var(--bg)`, making white-on-dark button text render as dark-on-dark (invisible). Found on AutoAcquire, which has its CTA at the end of the content div rather than right after the subtitle. Fixed with a higher-specificity override in `styles.css` (`.project-detail__content a.project-cta`) rather than restructuring markup on every page — this fixes it site-wide, including any future page that follows the same pattern.
- **Session Memory:** clarified everywhere (meta description, og/twitter tags, subtitle, opening paragraph) that this is a Claude Code **and Codex** skill for managing memory across multiple projects — Vikas felt "per-project memory" undersold the multi-project framing.
- **AutoAcquire:** removed "The Handoff," "Safety Rails," and "What I Learned" sections (Supabase/agent-protocol internals — same "show off" category as the removed status blocks). Added a second CTA, "Try Bowldem Now" → bowldem.com, next to "View on GitHub."
- **Rook + Sony TV Skill + articles.html (Big Green Button essay):** stripped remaining tech-stack callouts from body prose (Python/YAML/REST API/JSON-RPC/"Claude Code and OpenClaw") — Vikas: "we decided to clean it up from everywhere," not just the deleted status blocks. Kept the functional descriptions, dropped the specific tech names.
- **Bowldem for Reddit:** removed the "What I Learned" section + callout (Devvit sandbox-limitations reflection) — same category.
- **Bowldem:** removed "Why I built it" section. Added nostalgia framing (clues walk you through eras/teams from cricket history) and a new "The daily newsletter" section — subscribers get a daily email at stumps with the answer + previous day's winner.
- **Plumo:** collapsed to a single line per Vikas's exact wording — "Prototype, never shipped. An AI-native pet simulator — pat, chat, and play mini games with Plumo, helping it understand the human world better." Removed Key Features / Vision sections. Homepage status badge changed "In dev" → "Prototype" to match.
- **Helicopter Game: removed entirely** — deleted `helicopter-game.html`, its homepage entry, and its `sitemap.xml`/`llms.txt` references.
- **Verified in-browser** (local `npx serve` on port 8795, PID-targeted kill after — no repeat of the earlier blanket `taskkill` incident): AutoAcquire's two CTA buttons render with visible white text; homepage Building list still renders as a clean 3-column grid ending at Plumo with the "Prototype" badge; no layout breakage from any of the section removals.

## Session — 2026-08-24 (image swaps + site-wide "show off" metadata removed)
- **Session Memory image swapped again.** Vikas supplied a direct URL for a clean line-art illustration (lamp lighting a notepad, orange bg) instead of the earlier GitHub OG card. Saved as `images/session-memory.jpg` (old `.png` deleted), wired into homepage thumb + `session-memory.html` OG meta.
- **Rook/OpenClaw image swapped.** Vikas supplied a Google-thumbnail URL for the actual OpenClaw logo/mascot (small red creature + wordmark on white, 598x360) to replace the GitHub repo screenshot. Same filename (`images/openclaw.jpg`) so no reference updates needed. Note: the square homepage `build-thumb` crop clips the wordmark to "penCla" since the source is a wide logo with a small centered mark — legible but not ideal; full-size hero on `rook.html` looks clean. Not revisited unless Vikas flags it.
- **Removed ALL `.project-status` blocks site-wide (13 detail pages) + all homepage `.build-metrics` tech-stack lines (8 entries).** Vikas: "we dont need this level of show off" — quoting Session Memory's Status/License/Stack block as the example of what he means. Interpreted as: strip status/license/tech-stack/platform metadata everywhere, but KEEP functional CTA links (View on GitHub, Play Now, View on Reddit, etc.) — unwrapped them from the now-deleted `.project-status` div since `.project-cta` styling doesn't depend on that parent. Sony TV Skill's "Part of: Rook AI Assistant" cross-link was kept as a plain sentence (useful navigation, not spec-sheet metadata) rather than deleted outright.
- **Verified in-browser:** no orphaned spacing/borders left behind on pages that lost the whole status block; CTA-only pages render cleanly with correct button spacing.
- **If more "show off" content surfaces later**, the same instinct applies: metadata/spec-sheet framing (License, Stack, Platform) is out; plain descriptive prose and functional links (play/view/GitHub) are fine to keep.

## Session — 2026-08-24 (build-thumb layout bug fix + Session Memory image swap)
- **Found and fixed a real layout bug.** Every `build-thumb` added across this session and the prior one (8 entries: Session Memory, AutoAcquire, Sony TV Skill, Rook, Bowldem for Reddit, Bowldem, Plumo, Helicopter Game) had been inserted as a sibling *before* `.build-rail` instead of nested inside it, after `.build-year`. `.build-item` in `styles.css` is a fixed 3-column CSS grid (`rail / body / arrow`) — a 4th top-level child broke the columns and squeezed `.build-desc` into a ~1.5rem column, wrapping text one word per line. Fixed all 8 to match the correct pattern already used by the pre-existing Port of Hormuz / FPL entries. Verified visually in-browser after the fix — homepage Building list now renders normally.
- **Session Memory image swapped.** Vikas supplied a direct image URL (a clean line-art illustration of a lamp lighting up a notepad, orange background: `cdn.prod.website-files.com/.../og-claude-skills.jpg`, 1200x630) to replace the GitHub OG card, which read poorly at small thumbnail size (mostly illegible repo-name text). Saved as `images/session-memory.jpg` (replacing the deleted `images/session-memory.png`), updated `index.html` build-thumb and `session-memory.html` OG/Twitter meta. Reads much more clearly at thumbnail size than the old OG card.
- **QA process note:** ffmpeg/asset generation is fine to verify by reading the file directly, but layout/CSS bugs are NOT reliably caught by pixel inspection of one card at a time — need to actually load the page in a browser and check adjacent grid columns render correctly, especially after touching shared grid/flex parents.
- **Video playback note:** embedded videos (Sony TV Skill, Plumo, RedFlag.exe) show a stuck loading spinner in the claude-in-chrome automated tab even on a range-request-capable server (tested with `npx serve` after ruling out Python's `http.server` lacking Range support) — traced to the automation Chrome instance's video decoder, not a real bug. Confirmed via ffprobe the files are valid faststart H.264/AAC, and Vikas confirmed the videos play fine in his own real browser. Don't rabbit-hole on this again — it's an environment limitation, not something to keep debugging.
- **Caution:** accidentally ran `taskkill /F /IM node.exe` while stopping a local test server, which killed ALL node processes system-wide (36 of them), not just the intended one. Use PID-targeted `taskkill /F /PID <pid>` instead, never the blanket `/IM node.exe` form, when a local dev server needs killing.

## Session — 2026-08-24 (thumbnails round 2 — all pending items closed)
- **Bowldem for Reddit — DONE.** Found the actual Reddit post URL (`reddit.com/r/playbowldem/comments/1r0mrdq/bowldem_daily_cricket_puzzle/`) by capturing a frame from the YouTube walkthrough video at a moment where the browser address bar was visible — the earlier live-playback capture approach kept failing, but jumping via `?t=Ns` URL param + pause worked. Cropped that frame into `images/bowldem-reddit.jpg`, wired into homepage `build-thumb` and `bowldem-reddit.html` (new `.project-detail__image` block, added "View on Reddit" CTA, upgraded twitter:card to `summary_large_image`).
- **Session Memory — DONE.** Re-fetched the GitHub OG card (`opengraph.githubassets.com/1/ssupppp/claude-session-memory`) since the original scratch copy from the prior session didn't survive. Saved as `images/session-memory.png`, wired into homepage `build-thumb` and `session-memory.html`'s OG/Twitter meta (page keeps its existing SVG diagram as the on-page visual, no `.project-detail__image` added there).
- **AutoAcquire — DONE.** Vikas manually dropped `images/autoaquire2.jpg` (a Meta ad-creative screenshot) into the repo as a stand-in since no real dashboard exists (confirmed last session). Wired into homepage `build-thumb`, added a new `.project-detail__image` block to `autoacquire.html`, and set `og:image`. Note: source image is small/portrait (287x498) — fine as a placeholder per Vikas's "for the time being" framing, worth revisiting with a better asset later.
- **Sony TV Skill — DONE.** Embedded existing `images/sony-tv-netflix.mp4` into `sony-tv-skill.html` via the standard `<video poster=...>` pattern. Extracted a poster still (`images/sony-tv.jpg`, frame at 0:04) that happens to show the actual "Openclaw ai" WhatsApp chat controlling the TV — used as poster, homepage `build-thumb`, and OG image.
- **Bowldem — DONE.** Screenshotted the live game at bowldem.com, cropped to 16:9 (`images/bowldem.jpg`), replaced the stock Unsplash placeholder image that was in `bowldem.html` (both the OG/Twitter meta tags and the inline `<img>` — they used different query-string URLs so needed two separate edits) and wired into the homepage `build-thumb`.
- **Rook / OpenClaw — DONE.** Vikas asked to "get openclaw from the open web" — WebSearch confirmed OpenClaw is a real, very popular (387k GitHub stars) open-source personal-AI-agent project at `github.com/openclaw/openclaw` (site: openclaw.ai), which matches the existing memory note that Rook is "set up via OpenClaw." Screenshotted the GitHub repo page, cropped to `images/openclaw.jpg`, used it as `rook.html`'s new `.project-detail__image` + OG image + homepage `build-thumb` (Rook didn't have a thumb before either).
- **Process note confirmed again:** for YouTube frame grabs, live playback screenshots are unreliable (video won't buffer/autoplay drifts between calls) — navigating directly to `?t=Ns`, waiting, then screenshotting with `save_to_disk` in one shot (no intermediate look-first screenshot) is the reliable pattern; an intermediate screenshot call gives autoplay enough time to jump to the next video.
- **Status: all originally-requested thumbnail/asset work for this feature is now closed.** Nothing committed yet — still uncommitted on `preview`.

## Session — 2026-08-24 (thumbnails + video loops)
- **Plumo — DONE.** Homepage `build-thumb` wired to `images/plumo.png`. Upgraded `plumo.png` itself: was a low-res 533x528 crop, replaced with a proper high-res (1170x1170) still pulled from Vikas's own gameplay recording (`D:\personal\videos\Plumo.MP4`, frame at 3:47). Detail page (`plumo.html`) now embeds a video loop (`images/plumo-loop.mp4`, ~67s, 640px, muted, autoplay/loop/controls, same `<video poster=...>` pattern as `port-of-hormuz.html`) instead of the static image. Loop built from 3 timestamp ranges Vikas specified (58-70s, 3:35-4:00, 6:30-7:00), concatenated + compressed to ~1.9MB via ffmpeg.
- **Helicopter Game — DONE.** Homepage `build-thumb` wired to existing `images/helicopter_game.jpg` (asset already existed, was just never linked from the homepage row).
- **RedFlag.exe — DONE (video only).** Detail page (`redflag.html`) now embeds a video loop (`images/redflag-loop.mp4`, ~38s, 640px, muted) built from 4 timestamp ranges from `D:\personal\videos\RedFlag-Ending1.MP4` (22-30s, 1:05-1:20, 2:27-2:32, 4:00-4:10). **Kept the existing `images/redflag.jpg` key-art image as poster/thumbnail** — Vikas confirmed it's better than any extracted gameplay frame. Note: RedFlag is not currently a homepage Building-list entry (only referenced inline in body text) — that's pre-existing scope, not touched this session.
- **Session Memory — asset found, not yet applied.** Downloaded the GitHub social/OG card for `ssupppp/claude-session-memory` (`https://opengraph.githubassets.com/1/ssupppp/claude-session-memory`, 1200x600 PNG) to scratch. Awaiting Vikas's go-ahead to save into `images/` and wire into the homepage thumb + `session-memory.html`.
- **AutoAcquire — no dashboard exists, confirmed via docs.** Checked `D:\personal\bowldem\CLAUDE.md`, migrations, and the `autoacquire-agent` edge function — AutoAcquire is a Supabase cron edge function + Meta Marketing API integration, **not a hosted web app with its own UI**. The only "dashboard" references in the docs are the generic Supabase project console and Vercel's deploy dashboard — neither is AutoAcquire-specific. A real thumbnail would have to be a screenshot of either the Supabase table/state view or Meta Ads Manager showing its campaigns. Vikas still deciding which; blocked until he either sends a screenshot or gives access/URL.
- **Still pending this round:** Bowldem for Reddit (frame grab from YouTube hackathon submission, https://www.youtube.com/watch?v=pBj0nGyH-f0), Bowldem (screenshot from bowldem.com), Rook (explicitly deferred by Vikas — "just a note for now," needs its own scoping conversation about an "openclaw + rook merged" screenshot).
- **Process note for future asset work:** for long source videos, don't guess at "the key bit" — ask Vikas for explicit timestamp ranges per segment, then trim+concat+compress via ffmpeg (works well, keep using this pattern). Also don't build heavy-token contact-sheet previews to select frames — Vikas flagged this as unnecessary token spend; just ask for timestamps directly.

## Session — 2026-08-24
- **Infrastructure correction:** discovered (via `curl -I` on the live site + `gh api repos/ssupppp/vikasmenon/pages`) that the site runs on **GitHub Pages** (build from `main`, root path, custom domain via `CNAME`), not Vercel — CLAUDE.md's entire branch-model section was wrong and has been corrected. There is no live preview/staging deploy of any kind; `preview` is just a normal branch. Local static server (`python -m http.server`) is the only way to look at unshipped work today.
- **Homepage design pass #2** (first pass — placeholder-box removal + AutoAcquire title — was 2026-08-23):
  - **Removed the hero entirely.** Vikas: "don't want to show off." `.hero`/`.hero-title`/`.hero-lede` deleted from `index.html` + `styles.css`; page now opens directly on the Building list. Added `.visually-hidden` utility + an sr-only `<h1>` so the page keeps exactly one real h1 for SEO/accessibility without any visual footprint. New `.section--first` class gives the first section (`#building`) its own top clearance from the fixed nav (9rem desktop / 7rem mobile) and no top border, replacing what the hero used to provide.
  - **Reordered Sony TV Skill above Rook.** It's a skill built *for* Rook, but strict date-sort (2025 vs Rook's "Mar 2026—") had separated it to the very bottom of the list, below Plumo/Helicopter Game — Vikas flagged this as "the timeline looks off." Fixed by grouping it directly above its parent, overriding pure chronological order for this one relationship.
  - **Fixed the Building→Writing transition.** Root cause: `.build-item:last-child`'s bottom border + the next section's own top border sat close together, reading as an accidental doubled rule. Removed the redundant one — now a single hairline, same section-padding rhythm as every other transition on the page.
  - **Compacted the featured essay.** `.essay--featured` was running at clamp(2rem–2.85rem) title / 1.15rem dek / 4rem margins — dramatically bigger than the regular list (1.5rem/1rem). Brought it down to match the list's scale almost exactly (1.5rem title, 1rem dek, ~1.75rem margins); it's still visually first by position and a border-bottom separator, not by oversized type. Vikas's stated principle: "don't oversize things like headers — they look cheap and showoffish."
  - Verified all four fixes live via local server + browser screenshots. **Not yet committed** — sitting as uncommitted changes on `preview` alongside the docs correction above.
- **Homepage thumbnails — plan, not yet executed.** Vikas wants real thumbnail images back on the Building list (not the empty-box placeholder we removed, and not permanently boxless either). Confirmed one source image per project is enough — same file serves the small homepage crop and the larger detail-page hero crop via `object-fit: cover`, no need to shoot two images per project.
  - **Ready to wire up now, assets already exist:** Plumo (`images/plumo.png`, already used as `plumo.html`'s hero image) and Helicopter Game (`images/helicopter_game.jpg`, already used as `helicopter-game.html`'s hero image). Just needs the homepage `build-thumb` markup added back for these two, pointing at the existing files — no new asset sourcing.
  - **Blocked on real assets from Vikas:** Session Memory, AutoAcquire, Rook, Bowldem for Reddit, Bowldem, Sony TV Skill — none have any image anywhere in the repo. Do not fabricate placeholders for these; wait for Vikas to supply or point at something real (screenshots, logos, a still from the existing unused `images/sony-tv-netflix.mp4` for the Sony TV Skill entry, etc.). Rook is private/no public demo, may end up staying imageless by choice.
- **Session end state — NOTHING IS LIVE.** Confirmed via git: `preview` is 1 commit ahead of `origin/main` (the 2026-08-23 Blue Revision commit, pushed to `preview` only), plus 4 files currently uncommitted locally (`CLAUDE.md`, `MEMORY.md`, `index.html`, `styles.css` — today's design pass #2 + doc corrections). GitHub Pages only ever serves `main`, so none of this session's work — Blue Revision rebuild, design passes, or doc corrections — has reached the live site at vikasmenon.com. Explicit Vikas confirmation ("this is not live right now") — do not describe any of this as shipped/live in future sessions until an actual `ship` (fast-forward main → push) happens.
- **Next session priorities:**
  1. Wire up Plumo + Helicopter Game homepage thumbnails (assets ready, zero blockers).
  2. Get real images from Vikas for the other 6 Building items, or decide which stay imageless.
  3. Commit + push the current uncommitted round (hero removal, reorder, seam fix, featured compact, GitHub Pages doc correction) to `preview`.
  4. When ready: `ship` (fast-forward `main` from `preview`) to actually go live — has not happened yet this entire rebuild.
  5. Mobile visual QA still outstanding (never confirmed via real screenshot, only code review).

## Backlog / Ideas (no formal BACKLOG.md for this project — tracked here)
- **Session Memory** — ✅ done 2026-08-23. New Building entry + `session-memory.html` detail page (self-authored SVG diagram, no external assets). GitHub: github.com/ssupppp/claude-session-memory.
- **FACS videos entry** — raised 2026-08-23, no details yet (what it is, status, links/assets). Ask Vikas before scoping.
- **About-me timeline** — raised 2026-08-23. Sounds like an IA idea for the Now/About section (visual timeline), not a single project entry — needs a scoping conversation (what milestones, how far back, static vs. interactive) before planning.

## Session — 2026-08-23
- Worked on: Full Blue Revision redesign execution, per the Build Sheet decided in an earlier planning session (docs live as Claude Artifacts, not in this repo). Branch: `feat/blue-revision-rebuild` (off `main`, not `preview` — see note below).
- Completed:
  - Retain: homepage Building-row thumbnails (real crop for Port of Hormuz + Fantasy Team Rater, honest hatched placeholder for the rest) via new `.build-rail`/`.build-thumb` CSS; Port of Hormuz + Fantasy Team Rater case-study callouts (`.project-callout`) using existing verbatim lines.
  - Improve: articles.html rebuilt with a two-column TOC (CSS `columns: 2`, no JS) and re-sorted every essay strictly by date, newest first (was previously out of order — AI-Native Games, the oldest essay from Nov 2024, was incorrectly the "featured" pick on both articles.html and the homepage Writing section; fixed to feature the actual newest essay, "Goodbye to the Big Green Button," Mar 2026). Callouts added to AutoAcquire (₹2,068 line), Bowldem for Reddit ("platform constraints" line), Rook ("private, no public demo" status line). Bowldem's player-count stat moved from the homepage row into the page body.
  - Tried and reverted: a "featured essay" treatment (drop cap + pull-quote component) — Vikas called it "pretty cringe," fully removed from both HTML and CSS. Note for future: don't reach for drop-caps/pull-quotes as a default editorial device on this site.
  - Snackula + RedFlag.exe: added "no public link available" status line to both (previously said "Live on Ripple AI" with no way for a visitor to actually reach it). Vikas may upload one or both somewhere public later — swap the line for a real link when he does.
  - New homepage entry: **Session Memory** (`session-memory.html`) — open-source Claude Code skill Vikas published (github.com/ssupppp/claude-session-memory). Own detail page with a self-authored inline SVG diagram.
  - **Kill list — DONE.** All six files removed: `portfolio.html`, `projects.html`, `work.html`, `playbowldem-reddit.html`, `script.js`, `content.txt`. Before deleting the three hub pages, checked every project tile inside them for exclusive inbound links — Plumo, Helicopter Game, Sony TV Skill, and Rizz Messenger only had the hub pages linking to them, so all four were promoted to proper homepage Building entries first (design call: **no separate `/projects` page going forward — homepage Building list is the single source of truth**, consistent with the 3-item nav rule in CLAUDE.md). `sitemap.xml` cleaned of every killed page in the same pass.
  - **Decide items — DONE.**
    - Helicopter Game / Plumo / Sony TV Skill: promoted to Building (see above).
    - **ValuePickr Chat + Rizz Messenger: hidden, not deleted** — Vikas called both "incomplete projects." Removed from homepage Building list and `sitemap.xml`; `rizz-messenger.html` file kept on disk, unlinked (ValuePickr never had its own page). Recoverable later if either project reaches a "done" state worth surfacing.
    - **Resume PDF: unlinked, not deleted.** Read the actual PDF — it's a different professional pitch than the site (traditional live-ops/product-manager resume: "Terrabyte," "Head of Games & Product," D1/D7 retention metrics, no mention of PlotPix.ai or AI at all) vs. the site's AI-native-builder brand. Flagged as a positioning conflict; Vikas chose to unlink rather than rewrite or keep. File stays on disk at `Vikas_Menon_Resume.pdf`, just not linked from the contact strip.
  - **sitemap.xml + llms.txt — DONE, fully rebuilt.** sitemap.xml now lists exactly the live, linked pages (added session-memory.html, autoacquire.html, bowldem-reddit.html; excludes hidden ValuePickr/Rizz Messenger and all six killed files). llms.txt Site Map now points at real anchors (`#building`, `#now`) instead of the dead hub pages; Projects list matches the actual homepage; PlayBowldem entry now links to `bowldem-reddit.html` instead of a GitHub URL; Work section corrected from stale "Head of Games & Platform" to current "Head of Content & AI"; Resume line removed.
- Decisions made:
  - Blue Revision direction locked (see Artifacts, not in this repo). Real-repo work happens on a feature branch (`feat/blue-revision-rebuild`), not this repo's usual `preview` branch, because the scope is large and multi-page — deliberate deviation from the CLAUDE.md branch model, to be reconciled once the rebuild is further along.
  - **No standalone `/projects` or `/work` hub page.** The homepage Building list is the sole canonical listing going forward — two separate grid pages (`portfolio.html`, `projects.html`) had each drifted stale relative to the homepage by the time this session found them. One source of truth, not three.
  - **"Hide, don't delete" is the default for incomplete/borderline content** (ValuePickr Chat, Rizz Messenger, Resume PDF) — Vikas's stated preference: unlink from nav/sitemap, keep the file on disk, recoverable later.
  - **Browser QA (desktop) — DONE.** Verified in Chrome: homepage Building list (all 10 entries, correct order, ValuePickr/Rizz Messenger correctly absent), Session Memory detail page + inline SVG diagram, Now section/contact strip (Resume cleanly removed), articles.html two-column TOC ordering. Mobile viewport resize did not reflect in the browser tool — mobile layout verified by code review only (the `max-width:720px` breakpoint), not by screenshot.
  - **Design pass — DONE.** Vikas flagged "boxes seemed too large" after seeing the real render. Root cause: 7 of 10 Building entries showed an empty hatched-placeholder thumbnail box (only Port of Hormuz + Fantasy Team Rater have real images), which read as unfinished rather than intentional. Fix: removed the placeholder box entirely for all 7 (`build-thumb--empty` CSS rule deleted, markup removed) — those rows are now clean text-only, matching pre-thumbnail style. Also fixed "AutoAcquire — Agent-Driven Ad Engine" (the only subtitle-in-title entry) wrapping and orphaning its status pill onto its own line — shortened to just "AutoAcquire" to match every other entry.
  - **Branch reconciliation — DONE.** No `preview` branch existed in this repo (only `main` + the feature branch, both at the same commit). Vikas chose to adopt `feat/blue-revision-rebuild` as the new `preview`: committed all changes (`5de04d8`), renamed the branch, pushed to origin. Vercel's webhook should auto-deploy the preview URL.
- Next session priorities:
  1. Exercise the live Vercel preview URL to confirm the deploy landed, then `ship` (fast-forward `main` from `preview`) when Vikas is ready.
  2. Mobile visual QA still outstanding — verify the `max-width:720px` breakpoint actually renders correctly on a real small viewport (browser tool's resize wasn't reflecting this session).
  3. Backlog items awaiting Vikas's input: FACS videos entry (not ready, no details yet), About-me timeline (needs a scoping conversation) — see Backlog / Ideas section above.
  4. Flagged but not fixed (pre-existing, sitewide, out of scope for this pass): small metadata type size (~10.5-11px), `--text-tertiary` under WCAG AA contrast on `--bg`.
